/**
 * Configuración del cliente. Único lugar donde se define la URL base
 * del backend, para no repetirla ni "quemarla" (hardcodear) en cada
 * llamada fetch.
 */
const CONFIG = {
  API_BASE_URL: "http://localhost:5000",
};
